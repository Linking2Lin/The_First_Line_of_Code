package com.example.servicetest.kotlins.changes.covariate

/*
fun main() {
    val student = Student("Tom",277)
    val data = SimpleData<Student>()
    data.set(student)
    handleSimpleData(data)//因为该函数接收的是一个SimpleData<Person>对象，并不是一个SimpleData<Student>，所以报错
    val studentData = data.get()
}

fun handleSimpleData(data: SimpleData<Person>): Unit {
    val teacher = Teacher("Jack",999)
    data.set(teacher)
}

 */

fun main() {

}
